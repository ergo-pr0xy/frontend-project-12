import React from 'react';

const NotFoundPage = () => (
  <div className="not-found-container">
    <div className="d-flex flex-column h-100 margin-top">
      <div className="text-center">
        <h1>Упс...</h1>
        <p>Похоже, такой страницы не существует</p>
      </div>
    </div>
  </div>
);

export default NotFoundPage;
